using UnityEngine;
using System.Collections;

public class Game : MonoBehaviour {
	public AudioSource audio;
	float[] samples ; 
	int size = 2048;
	// Use this for initialization
	void Start () {
		//samples = new float[size];
		// StartCoroutine(mussic  ());
	}
	
	// Update is called once per frame
	void Update () {
	 
		
    
	}
	
}
