using UnityEngine;
using System.Collections;

public class ButtonHeal : MonoBehaviour {
	public float intervalo;
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		//while(transform.localScale < new Vector3(2f,2f,2f))
		//iTween.ScaleBy (this.gameObject, new Vector3(1.1f,1f,1.1f),0.1F);
	
	}
}
